# Relay Window is a dev tool: always revalidate so edits show up on reload.
server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / {
    add_header Cache-Control "no-cache";
    try_files $uri $uri/ =404;
  }
}
